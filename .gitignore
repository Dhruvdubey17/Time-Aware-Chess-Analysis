# Local working files, never tracked
PLAN.md
context.md
BUILD.md
BUILD2.md

# All runtime data (downloads, parsed parquet, feature tables)
data/

# Chess engine binary fetched by the install script, not committed
engines/

# Release archive built by install/prepare_release.sh, never committed
chess-review.tar.gz

# Python
.venv/
.venv_maia/
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.ruff_cache/

# macOS
.DS_Store

# Notebook checkpoints
.ipynb_checkpoints/
